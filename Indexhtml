<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>n.ai.in – Smarter Than Google</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      margin: 0;
      padding: 0;
      text-align: center;
      background-color: #fff;
      color: #202124;
    }

    header {
      padding: 20px;
      border-bottom: 1px solid #ccc;
    }

    h1 {
      font-size: 32px;
      margin-bottom: 8px;
      color: #1a73e8;
    }

    #search-box {
      margin: 20px auto;
      max-width: 600px;
      display: flex;
      justify-content: center;
      gap: 10px;
    }

    #query {
      flex: 1;
      padding: 14px 20px;
      font-size: 16px;
      border-radius: 30px;
      border: 1px solid #ddd;
      width: 100%;
    }

    button {
      background-color: #1a73e8;
      border: none;
      padding: 12px 14px;
      border-radius: 50%;
      color: white;
      font-size: 16px;
      cursor: pointer;
    }

    #result {
      margin-top: 30px;
      padding: 20px;
      max-width: 700px;
      margin-inline: auto;
      background: #f8f9fa;
      border-radius: 10px;
    }

    nav {
      margin: 30px 0 10px;
    }

    nav a {
      margin: 0 10px;
      text-decoration: none;
      color: #1a0dab;
      font-size: 16px;
    }

    .news-section {
      margin-top: 40px;
    }

    .news-image {
      width: 100%;
      max-width: 300px;
      border-radius: 10px;
    }

    footer {
      margin-top: 40px;
      padding: 20px;
      font-size: 14px;
      color: #666;
    }
  </style>
</head>
<body>
  <header>
    <h1>n.ai.in</h1>
    <small>Smarter Than Google</small>
  </header>

  <div id="search-box">
    <input type="text" id="query" placeholder="Search smarter..." />
    <button onclick="search()">🔍</button>
  </div>

  <div id="result">Your AI answers will appear here...</div>

  <div class="news-section">
    <h2>📰 Latest News</h2>
    <img src="https://via.placeholder.com/300x150?text=News" class="news-image" alt="News">
    <p>AI is changing the world. Read how <strong>n.ai.in</strong> is leading the revolution!</p>
  </div>

  <nav>
    <a href="#">🏠 Home</a>
    <a href="#">🔎 Search</a>
    <a href="#">🔔 Notifications</a>
    <a href="#">📊 Activity</a>
    <a href="#">🧠 AI Codes</a>
  </nav>

  <footer>
    © 2025 <strong>n.ai.in</strong> by Nizam – Smarter Than Google
  </footer>

  <script>
    async function search() {
      const query = document.getElementById("query").value;
      if (!query) return;
      document.getElementById("result").innerText = "🔍 Thinking...";

      try {
        const res = await fetch(`/api/search?q=${encodeURIComponent(query)}`);
        const data = await res.json();
        document.getElementById("result").innerHTML = data.answer || "No answer.";
      } catch {
        document.getElementById("result").innerText = "⚠️ AI not available.";
      }
    }

    document.getElementById("query").addEventListener("keypress", e => {
      if (e.key === "Enter") search();
    });
  </script>
</body>
</html>
